
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Recommended: Allow read and write access only for authenticated users.
    // This is secure for the admin panel as only an admin can be logged in to upload.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
